[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    services: tests that exercise app/services/* pure functions
    repositories: tests that exercise app/repositories/* wrappers
    boundary: edge-case / boundary-value verification
